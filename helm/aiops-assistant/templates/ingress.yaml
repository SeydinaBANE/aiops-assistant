{{- if .Values.ingress.enabled }}
apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: {{ include "aiops-assistant.fullname" . }}
  labels: {{ include "aiops-assistant.labels" . | nindent 4 }}
spec:
  rules:
    - host: {{ .Values.ingress.host }}
      http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: {{ include "aiops-assistant.fullname" . }}
                port:
                  number: {{ .Values.service.port }}
{{- end }}
